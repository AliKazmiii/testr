[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyjs"
version = "0.1.0"
description = "Python port of index.js functionality (partial)."
authors = [{name = "Converted", email = "you@example.com"}]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
pyjs-preinstall = "pyjs.cli:main"

